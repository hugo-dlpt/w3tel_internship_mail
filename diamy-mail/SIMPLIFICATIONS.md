# Simplifications assumées de la maquette

Ce fichier distingue « pas encore fait » de « fait faux ». À tenir à jour : c'est ce qui
rend la maquette **honnête** en revue. Règle d'or (guide §12) : si tu modifies la conception,
tu mets à jour l'annexe concernée (version + changelog) — jamais de divergence code/spec.

| Domaine | Simplification actuelle | Raison | Ce que fera la vraie implém | Annexe |
|---------|-------------------------|--------|-----------------------------|--------|
| Adresse (A24) | Parseur RFC 5322 addr-spec/display-name = sous-ensemble minimal (pas de domain-literal `[...]`, pas de commentaires `(...)`) | Périmètre maquette | Parseur RFC 5322 complet ou crate dédiée auditée | A24 |
| Adresse (A24) | RFC 2047 : charset traité comme UTF-8, encodages `B`/`Q` seulement | Périmètre maquette | Support multi-charset complet | A24 |
| Adresse (A24) | Casefold "insensitive" via `str::to_lowercase()` Rust (approx. du full case folding Unicode) | Pas de crate de case-folding Unicode dans le workspace | Unicode full case folding conforme (ex. `ß`→`ss`) | A24 |
| Adresse (A24) | `MIXED_SCRIPT_*` : heuristique par plages Unicode (Latin/Cyrillic/Greek/Han), pas l'algorithme UTS #39 complet | Périmètre maquette | Détection UTS #39 complète (tables de scripts + Common/Inherited) | A24 |
| Adresse (A24) | `CONFUSABLE_DOMAIN` et `PUNYCODE_LOOKALIKE` toujours à 0 (non implémentés) | Nécessitent respectivement l'historique de correspondants on-device et une liste de marques versionnée (A24 §11 : gouvernance à définir avec A06) | Détection réelle une fois ces dépendances disponibles | A24/A06 |
| Adresse (A24) | Résolution IAM : une seule passe avec la politique plateforme par défaut, pas la 2e passe avec la politique du tenant résolu (A24-POL-3) | Périmètre maquette (`DevIamClient` en mémoire, pas de mapping domaine→tenant réel) | Résolution deux passes complète une fois le mapping domaine→tenant branché | A24/A17 |
| Crypto | Backend `dev-crypto` (RustCrypto) derrière `diamy-mail-crypto` | Messaging Diamy tout juste en ligne, pas stable | Backend `messaging-crypto` (primitives auditées messaging) + re-provision (pas d'interop) | A02/A18 |
| Signature identité | Ed25519 (stand-in) | Crate ML-DSA jeune | ML-DSA-65 (FIPS 204) | A02/A17 |
| IAM | `DevIamClient` en mémoire, principaux `@w3.tel` amorcés | Env de dev IAM à brancher | Adaptateur vers l'API de l'env de dev IAM, puis prod | A17 |
| Révocation | Non implémentée — s'applique aussi au jeton mail-plane de l'API de sync (`diamy-maild/src/auth.rs`) : signature HS256 + expiration vérifiées, PAS l'état de révocation | Mécanisme non confirmé (A17-TOK-2) | Selon mécanisme IAM confirmé | A17 |
| Sync (A04) — jeton mail-plane | Schéma des claims JWT (`sub`=`principal_id`, `exp`, `iat` uniquement) : ni A04 ni A17 ne le spécifient (seulement l'algorithme HS256, le secret `MAIL_JWT_TOKEN`, la durée 15 min) — choix de convention, pas une lecture du corpus. En l'absence d'IAM réel capable d'émettre un jeton, les tests et exemples LISENT un jeu de jetons **pré-signés une fois hors du code** (`tests/fixtures/dev_mail_plane_tokens.json`) : aucune fonction du projet ne sait en fabriquer (voir la ligne « Correction appliquée — capacité de fabrication de jeton de session retirée » ci-dessous) | Aucun IAM réel dans cette maquette pour émettre un vrai jeton mail-plane | Schéma de claims confirmé par IAM ; jetons réellement émis par IAM, jamais par `diamy-maild` (A17-P-1) | A04-TR-2/A17-TOK-1 |
| **Correction appliquée — capacité de fabrication de jeton de session retirée du code** | **INV-9 / A17-P-1** (« Diamy Mail MUST NOT mint identity or session tokens » — seul IAM émet des jetons de session) : la fonction `mint_dev_mail_plane_token` fabriquait elle-même, à la volée, un jeton de session HS256 valide DANS le code du projet. Bien que feature-gated (`#[cfg(test)]`/`feature="dev-token-issuer"`, absente du binaire de prod), c'était une violation **littérale** de la règle, gating ou non ; et le simple fait qu'une fonction sache signer un jeton valide reste un risque en soi (mal utilisée, copiée ailleurs, ou activée par erreur un jour). Ce point était auparavant une **ESCALADE ouverte** laissée à l'arbitrage de Cédric, avec deux options envisagées : (a) accepter le gating, ou (b) exiger un autre mécanisme — « fixtures pré-signées hors code de production, jamais une fonction `mint_*` ». **Ce point N'EST PLUS une escalade** : la correction **RETIRE** la violation au lieu de la justifier (elle réalise l'option (b)), donc aucun arbitrage de conception n'est nécessaire — **Hugo l'a prise en charge directement, sans remonter à Cédric** (à la différence d'un vrai conflit invariant/conception, qui, lui, s'escalade sans coder autour, A25 règle 4). **Corrigé** : (1) `mint_dev_mail_plane_token` ENTIÈREMENT supprimée (la fonction ET son re-export), et le feature `dev-token-issuer` retiré des 3 `Cargo.toml` (il ne servait qu'à elle) — il ne reste **aucune capacité de SIGNER un jeton** dans le code (`jsonwebtoken` n'est plus utilisé que via `DecodingKey`, pour VÉRIFIER : `verify_mail_plane_token`). (2) Un jeu de jetons de test **pré-signés une seule fois hors du code** (calcul HMAC-SHA256 one-shot, script jamais versionné) est figé en clair dans `tests/fixtures/dev_mail_plane_tokens.json` : jetons valides hugo/cedric/aubin (`exp` très lointain, ne périment pas) + un jeton expiré pour tester le rejet ; en-tête du fichier explicite (« ne jamais utiliser hors dev/test ; aucune fonction ne doit savoir les régénérer »). (3) Tous les appelants basculés sur la LECTURE de la fixture : tests unitaires de `mail_plane_token.rs`, tests d'intégration de `sync_api.rs`, exemple `read_test_mail.rs`. Preuve : test anti-régression **repo-wide** `crates/diamy-mail-iam/tests/no_token_minting_in_repo.rs` (`no_function_in_repo_can_mint_a_session_token`) qui scanne TOUT le repo (crates + services + exemples, pas un seul binaire, contrairement à `no_keygen_in_binary.rs`) et échoue si une capacité de signature de jeton (la clé de signature `jsonwebtoken`, l'appel d'encodage JWT) ou le nom retiré réapparaît. Vérifié : tests `diamy-mail-iam` verts (dont la vérification des jetons de fixture et le scan anti-régression) ; grep global → aucune fonction d'émission de jeton hors les jetons figés de la fixture | — | — | INV-9/A17-P-1 |
| **Correction appliquée — génération de clé isolée hors du binaire de service** | **INV-4 / INV-9 / A17-KEY-2** : `diamy-maild::main()` appelait INCONDITIONNELLEMENT `run_vertical_slice_demo`, qui générait une paire de clés d'identité ET une paire de clés d'appareil (`generate_identity_keypair`/`generate_device_keypair`) DANS le binaire de service, puis publiait un bundle — sur le chemin d'exécution NORMAL, sans aucune protection structurelle (contrairement à `mint_dev_mail_plane_token`, gaté par `#[cfg(...)]`). Violation LITTÉRALE d'A17-KEY-2 (« clés d'appareil générées UNIQUEMENT sur l'appareil client ») ; le fait que ça ne « fonctionne » pas encore en vraie cible tenait seulement au backend `messaging-crypto` renvoyant `not_wired()` — un hasard temporaire, pas une garantie. Gap **non tracé jusque-là**, signalé bloquant par l'audit. **Ce point N'EST PAS une escalade de conception** (au même titre, désormais, que la correction `mint_dev_mail_plane_token` de la ligne ci-dessus, elle aussi close directement puisqu'elle retire la violation) : c'est l'alignement du code sur un principe déjà non-négociable ; Hugo l'a pris en charge sans remonter à Cédric. **Corrigé** : (1) `run_vertical_slice_demo` entièrement RETIRÉE de `main()` (le service ne génère plus aucune clé, quel que soit le mode) ; (2) la démo du chemin vertical déplacée dans l'exemple Cargo `crates/diamy-mail-storage/examples/vertical_slice_demo.rs` (sur le modèle d'`enroll_test_device` — un exemple n'est JAMAIS lié dans le binaire de service) ; (3) les tests d'intégration de l'API de sync (`sync_api.rs`) alimentaient déjà leurs propres données de démo via des fixtures `#[cfg(test)]` (`enroll_device_for_test`/`store_test_message`) — jamais via `main()`, donc rien à changer côté tests. Preuve : test anti-régression `services/diamy-maild/tests/no_keygen_in_binary.rs` (`service_main_never_generates_keys`) qui échoue si `generate_*_keypair` ou `run_vertical_slice_demo` réapparaît dans `main.rs`. Vérifié : `cargo check -p diamy-maild` OK, `cargo build --example vertical_slice_demo` OK, test vert ; grep global → plus aucun appel keygen hors exemples/`#[cfg(test)]` | — | — | INV-4/INV-9/A17-KEY-2 |
| Sync (A04) — AppKey Tier 2 | `X-App-Version` est exigé PRÉSENT sur chaque requête mais son intervalle n'est PAS vérifié (une seule AppKey de dev amorcée, `AppKeyStore::seeded_from_env`) | Tranche minimale : une seule AppKey/plateforme de test dans cette maquette | Vérification de l'intervalle de version configuré par clé, rotation double-slot (A17-APPKEY-7) | A17-APPKEY-4/5 |
| **Correction appliquée** | **INV-7 / A18-CRY-4** : `alg_version` (`mail.envelopes`) et `blob_alg_version` (`mail.blobs`/`mail.messages`) n'étaient NI écrits explicitement au scellement, NI contrôlés au déchiffrement — gap trouvé par l'audit du 2026-07-15, **non tracé jusque-là**. **Corrigé** : nouvel enum `diamy_mail_crypto::AlgVersion` (`V1`) ; version écrite EXPLICITEMENT au scellement (`store_inbound_message` renseigne les 3 colonnes, plus de `DEFAULT` implicite) ; lue et contrôlée AU DÉCHIFFREMENT via `AlgVersion::from_i32` (rejet fail-closed d'une version inconnue → `CryptoError::UnknownAlgVersion`, INV-16) côté serveur (`fetch_message_for_device`) ET côté client (le DTO de sync transporte la version ; `read_test_mail`/tests la re-contrôlent avant `open_*`). Le dispatch de déchiffrement (`open_message`/`open_with_key`/`unwrap_key`/`unwrap_message_key_from_hold`) est un `match` SANS catch-all — **4 sites de déchiffrement** — auxquels s'ajoute le `match` d'écriture/sérialisation `AlgVersion::as_i32` (**1 site d'écriture**) : ajouter une variante casse la compilation aux **5 sites au total** tant qu'elle n'est pas traitée (prouvé E0004). `hold_queue` n'a pas de colonne de version (DDL A21 §2.6) → `CURRENT` (donnée transitoire, emballée et désemballée dans la même version de code) ; ce n'est plus rattaché à une divergence ouverte (le design clé-seule est tranché, A21 v1.5). Testé : `unknown_alg_version_is_rejected_fail_closed`, `sealed_ciphertext_carries_current_alg_version` + round-trip Postgres réel (73 tests) | — | Un futur `V2` (bascule `messaging-crypto`) s'ajoute à l'enum et DOIT traiter chaque `match` | A02-CRY-7/INV-7/A18-CRY-4 |
| **Correction appliquée** | **INV-20 / A18-LOG-3** : aucun sink d'audit distinct des actions privilégiées (publications keydir, purges de hold) — gap de l'audit du 2026-07-15, **non tracé jusque-là**. **Corrigé** : `diamy_obs::audit::record(actor, action, before, after)` écrit dans un flux DÉDIÉ (fichier JSON-lines `DIAMY_AUDIT_LOG`, défaut `./audit_log/audit.jsonl`, gitignored + cible `tracing` réservée `diamy_audit`), jamais le log applicatif générique. Branché sur `publish_device_bundle` (actor = appareil signataire ; before/after = état + EMPREINTE SHA-512 tronquée de la clé PUBLIQUE, jamais la clé brute — INV-21), `purge_expired_holds` (une entrée par lot) et `release_held_messages_for_principal` (`hold.release`, une entrée par message relâché : IDs + compte d'enveloppes, jamais de clé/contenu). Testé : `record_appends_a_json_line…` + entrées réelles écrites pendant les tests d'intégration (`services/*/audit_log/`) | Tamper-evidence (chaînage/signature des entrées, A18-LOG-3 « where feasible ») pas encore là | Sink d'audit durable + tamper-evident en prod | INV-20/A18-LOG-3 |
| **Durcissement** | `#![forbid(unsafe_code)]` ajouté aux 3 services (`diamy-mxd`/`diamy-maild`/`diamy-submitd`) — était présent sur les 8 crates, absent des services (A18-CI-2). Aucun `unsafe` requis, compile proprement | — | — | A18-CI-2 |
| **Durcissement** | `#[derive(Clone)]` retiré de `MessageKey`/`DeviceEncSecretKey`/`IdentitySecretKey` : dupliquer du matériel secret est désormais une **erreur de compilation**, pas une simple observation (A18-ZERO-3, forbidden #4). Vérifié : aucun `.clone()` sur du secret n'existait (le seul `.clone()` porte sur une clé PUBLIQUE) | — | — | A18-ZERO-3 |
| AppKey Tier 2 — comparaison | Le lookup AppKey se fait par `SHA-256(clé brute)` puis `HashMap::get` (**hash-then-compare**), PAS `subtle::ConstantTimeEq`. **Choix défendable, pas un raccourci de sécurité** : la comparaison finale porte sur le digest 32 o, jamais sur la clé brute ; un canal temporel sur le hash ne révèle pas la clé (pas de préimage SHA-256 depuis un timing). Tag GCM et signature HS256 vérifiés en temps constant par `aes-gcm`/`jsonwebtoken`. Documenté dans `auth.rs::hash_key` | A18-ERR-4 vise le `==` sur secret **brut** ; le hash-then-compare est l'atténuation standard | Constant-time explicite si un jour un secret brut était comparé directement | A18-ERR-4 |
| **Correction appliquée** | L'enveloppe (`wrap_key_for_device`/`unwrap_key`) chiffrait `k_msg` sous `k_wrap` avec une AAD VIDE — la formule normative (A02-CRY-4, `"mailenv:"+message_id+":"+device_id`) n'était pas câblée (gap distinct de `body_ct`/`summary_ct`, déjà corrigé). **Corrigé** : `diamy-mail-crypto::aad_for_envelope(message_id, device_id)` (même discipline binaire-UUID que `aad_for_blob`/`aad_for_summary`, CDM-ID-2) ; `wrap_key_for_device`/`unwrap_key` prennent désormais un `aad: &[u8]` OBLIGATOIRE (signature changée, tous les appelants mis à jour : `diamy-mxd`, `diamy-maild`, `diamy-mail-storage::release_held_messages_for_principal`, `read_test_mail`). Testé : une enveloppe scellée pour UN (message, appareil) échoue à s'ouvrir sous l'AAD d'un AUTRE couple (tag GCM rejeté, `envelope_mismatched_aad_is_rejected`) ; vérifié aussi en conditions réelles (SMTP → Postgres → sync → déchiffrement) avec un message frais | — | — | A02-CRY-4 |
| Crypto — label HKDF de l'enveloppe (dev-crypto) | Le label HKDF qui dérive `k_wrap` de l'enveloppe (`INFO_ENVELOPE` dans `dev_backend.rs`) est un label STATIQUE (`"diamy-mail/dev-crypto/envelope-kek/v0"`), PAS le `info` normatif d'A02-CRY-4 (`"diamy_mail_env_v1" \|\| message_id(16) \|\| device_id(16)`). Le scoping par (message, appareil) EST bien assuré autrement — l'AAD `aad_for_envelope(message_id, device_id)` sur l'AEAD + le secret KEM `ss` distinct par clé publique d'appareil — donc INV-6 (label explicite, jamais de secret brut) reste respecté et le scoping cryptographique tient. Ce qui manque est précisément le *binding défense-en-profondeur* du `message_id`/`device_id` DANS le label HKDF, qu'A02-CRY-4 qualifie lui-même de « defense in depth ». **Signalé explicitement ici** car il était jusque-là seulement noyé dans la mention générale « labels HKDF dev-crypto provisoires » (ligne Crypto ci-dessus) | Backend `dev-crypto` provisoire, labels non-interopérables (re-provision à la bascule) | Le backend `messaging-crypto` câblera le `info` exact d'A02-CRY-4 (labels récupérés côté messaging) | A02-CRY-4/INV-6 |
| Sync (A04) — `tenant_id` | Dérivé en UUIDv5 depuis le domaine du destinataire (`diamy_mail_iam::derive_dev_tenant_id`, même pattern que `DevIamClient::seeded()` pour `principal_id`) — remplace l'ancien `Uuid::now_v7()` frais à chaque livraison (non déterministe) | Aucun mapping domaine→tenant réel dans cette maquette | Mapping domaine→tenant réel une fois A11 (onboarding) branché — ce champ reste un **holder inerte** jusque-là | A11/A17-P-3 |
| MX entrant (A01-QOS) | `MAX_DATA_BYTES` configurable via `DIAMY_MXD_MAX_DATA_BYTES` (défaut INCHANGÉ : 10 Mo) — A02-QOS-2 recommande 50 Mo par défaut. Préparé (variable d'env, testé) mais PAS activé : en attente de confirmation de Cédric sur si 10 Mo était un choix volontaire de maquette | Décision produit, pas technique — voir l'audit du 2026-07-13, point B | Valeur par défaut alignée sur A02-QOS-2 (50 Mo) une fois tranchée | A02-QOS-2 |
| Stockage (A21) | `diamy-maild` utilise un vrai Postgres : `mail.folders`/`messages`/`blobs`/`envelopes`/`hold_queue`/`journal`/`idempotency_keys` + `keydir.mail_device_keys` (A21 §3) — `journal`/`idempotency_keys` ajoutés par la migration `0005` (voir la ligne dédiée "état lu/supprimé" plus bas) | Périmètre maquette (tranche verticale) | Reste du schéma A21 : `search`, `send`, `onboard`, `cal`, `iam` | A21 |
| keydir (A17-KEY-3) | La signature du paquet d'appareil est vérifiée contre la clé d'identité fournie par l'appelant lui-même (`publish_device_bundle`), PAS contre un annuaire d'identité IAM réel | Aucun annuaire d'identité IAM (Dilithium) n'existe dans cette maquette | Vérification contre l'annuaire d'identité IAM réel avant acceptation du paquet | A17-KEY-3 |
| Stockage (A21) | Object store S3-compatible simulé par un répertoire local (`./blob_store/`) — Postgres ne garde que la référence `object_key`, conforme à A21 §1.1 | Pas d'infra S3-compatible dans la maquette | Vrai object store S3-compatible | A21/A02 |
| **Correction appliquée (effet de bord du chantier Bridge A20)** | `summary_ct` (A21-MSG-1) était scellé avec `crypto::seal_message(b"[resume non implemente - A08]", ...)`, qui tire une clé FRAÎCHE à chaque appel ; cette clé était aussitôt `drop()`ée SANS jamais être emballée dans une enveloppe par appareil (`wrap_key_for_device` ne portait que sur `message_key`, jamais sur `summary_key`). Conséquence : `summary_ct` n'était déchiffrable par **PERSONNE**, y compris avant ce chantier — un bug latent, pas une simplification assumée. **Trouvé et corrigé** en implémentant le Bridge (A20-IMAP-2 a besoin de pouvoir servir un Sujet à Thunderbird) : nouvelle fonction `crypto::seal_message_with_key` (symétrique d'`open_message`, qui accepte déjà une `&MessageKey`) ; `diamy-mxd::deliver_to_recipients`/`hold_recipient` scellent désormais le Sujet RÉEL (capturé par `diamy-mail-mime::ParsedMessage::subject`, RFC 2047 décodé) sous le MÊME `message_key` que le corps, AAD distincte (`aad_for_summary`, label `"mailsum:"` vs `"mailblob:"` du corps) — la MÊME enveloppe par appareil désemballe donc les deux. `fetch_message_for_device`/`FetchedDto` exposent maintenant `summary_ct`/`summary_nonce`/son `alg_version` au client. Testé : `seal_message_with_key_round_trips_and_is_domain_separated_by_aad` (round-trip + étanchéité AAD croisée) | Le rendu de résumé (A08, un extrait du corps plutôt que le seul Sujet) reste non construit — seul le Sujet est désormais porté par `summary_ct`, pas un résumé enrichi | Résumé réel (Sujet + extrait) généré côté client, chiffré, envoyé au serveur | A21/A08/A20-IMAP-2 |
| Stockage (A21) | Un seul rôle Postgres (`diamy`) pour tout — pas de séparation de rôles data-plane / control-plane (A21-X-1) | Une seule table active (`mail`) pour l'instant ; pas encore de schéma control-plane (`send`/`onboard`) à isoler | Rôles DB distincts par plan une fois `send`/`onboard` implémentés | A21-X-1 |
| Sync (A04) | Deux endpoints HTTP minimalistes (`GET .../messages`, `GET .../messages/:id`) sur `diamy-maild`, servis par `axum` — pas le protocole A04 complet | Tranche verticale fine | WSS + HTTPS, catalogue des endpoints complet (A04 §5), pagination, notifications | A04 |
| **Correction appliquée** | Absence totale d'authentification sur l'API de sync (ni AppKey Tier 2 ni jeton mail-plane, A04-TR-2/INV-25), trouvée par l'audit du 2026-07-13. **Corrigée** : middleware partagé `diamy-maild/src/auth.rs`, appliqué en `.layer(...)` sur tout le routeur — AppKey (lookup local, jamais d'appel IAM) validée AVANT le jeton mail-plane (signature HS256 + expiration), jamais l'inverse, jamais par endpoint (A17-APPKEY-5, A18-ERR-5). Le serveur reste par ailleurs lié à `127.0.0.1` uniquement (défense en profondeur). Restent des gaps distincts, voir les lignes "jeton mail-plane"/"AppKey Tier 2" ci-dessous | — | — | A04-TR-2/A17-APPKEY-5/INV-25 |
| Sync (A04) | Pas de WSS ni de notification signal-seul (A04 §1.2) : le client (Bridge) interroge directement la liste à chaque SELECT/STATUS/NOOP, il n'y a pas de signal à écouter avant de tirer. **`mail.journal` EST désormais écrit** (migration `0005`, voir la ligne "état lu/supprimé" plus bas) — chaque `flags_changed`/`message_deleted` y est réellement journalisé avec sa séquence — mais RIEN ne le consomme encore via un curseur : pas de `POST /sync/events`, pas de `ERR_CURSOR_EXPIRED`/full-resync (A04-SYNC-4) | Le curseur/la consommation du journal restent à construire ; seule son ÉCRITURE est réelle | Journal-cursor + notification WSS signal-seul (INV-12) | A04-SYNC-1 |
| Sync (A04) | Pas de pagination par curseur (A04-PAGE-1) : borne fixe (`LIMIT 50`) | Tranche minimale | Curseur `(principal_id, received_at, message_id)`, jamais `OFFSET` | A04-PAGE-1 |
| Sync (A04) | Le chiffré est renvoyé encodé en base64 inline dans le JSON, pas "par référence" (A00 API-3) | Tranche minimale, un seul message de test à la fois | Blobs fetchés séparément par ID, jamais inlinés | A00 API-3 |
| Sync (A04) | Le client de test (`read_test_mail`) garde sa clé privée d'appareil dans un fichier local `./dev_secrets/` (permissions 0600, gitignored) — stand-in du coffre sécurisé de l'OS, écrit une fois par `enroll_test_device` | Pas de vrai client desktop/mobile avec accès au Keychain/DPAPI | Vault client réel (A03), clé dans le coffre sécurisé natif de l'OS | A03/INV-4 |
| Sync (A04) | **HTTPS réel** (A04-TR-1) avec certificat auto-signé de dev (`rcgen`, régénéré à chaque démarrage) — pas une PKI réelle, un vrai client externe rejetterait ce certificat (vérifié : `verify error:num=18` avec un client TLS indépendant) | Pas de certificat signé par une autorité pour `w3.tel` dans cette maquette | Certificat réel (Let's Encrypt ou interne), rotation, chaîne de confiance | A04-TR-1 |
| Sync (A04) | Pas de signature de requête (A04-SIG-1). **L'idempotence (A04-EP-4/IDEM-1) EST désormais réelle** pour `/state/flags`/`/state/delete` (voir la ligne "état lu/supprimé" plus bas) — le gap qui reste ici est SEULEMENT la signature de requête, plus l'absence totale d'idempotence sur le reste de l'API (une seule mutation existe pour l'instant) | Tranche minimale | Signature liant méthode/chemin/corps/nonce/horodatage | A04-SIG-1 |
| Sync (A04) | Aucune observabilité sur l'API de sync (pas de compteur/latence, contrairement à A00 §11) | Tranche minimale | Compteurs `sync_*`, latence p99, health checks | A00 §11 |
| **Correction appliquée** | `fetch_message_for_device` ne vérifiait QUE `message_id`+`device_id`, jamais que le message appartenait au `principal_id` demandé — un appelant connaissant ces deux UUID pouvait lire le courrier d'un AUTRE principal. **Trouvé en relisant le code de l'API de sync, corrigé** : la requête SQL fait maintenant une jointure sur `mail.messages.principal_id`, testé avec un faux `principal_id` dans l'URL (→ 404) et le vrai (→ 200) | — | — | A04/INV-22 |
| **Correction appliquée** | `diamy-mxd` scellait `body_ct`/`summary_ct` **UNE SEULE FOIS pour tout l'envoi SMTP**, puis réutilisait le MÊME `k_msg`/chiffré pour CHAQUE destinataire d'un `RCPT TO` multi-destinataires — deux destinataires distincts d'un même envoi partageaient donc le même `k_msg` : l'appareil compromis de l'un aurait pu déchiffrer la copie de l'autre si le contenu coïncidait (violation A02-CRY-6, "deux `k_msg` indépendants"). **Trouvé en implémentant l'AAD obligatoire** (le `message_id` doit exister avant le chiffrement pour entrer dans l'AAD, ce qui a forcé à sceller PAR destinataire plutôt qu'une fois pour tous) **, corrigé comme effet de bord** : chaque destinataire a maintenant son propre `k_msg`/chiffré indépendant (`seal_message` tire une clé fraîche à chaque appel) | — | — | A02-CRY-6 |
| MX entrant (A01) | `diamy-mxd` sert un VRAI protocole SMTP (EHLO/MAIL FROM/RCPT TO/DATA/QUIT) sur un port non privilégié (2525), pas encore le port 25 avec un vrai MX public (A01, en cours avec Aubin) | Serveur MX de bureau pas encore monté | Vrai MX public sur le port 25, DNS/MX pointant dessus | A01 |
| MX entrant (A01) | **STARTTLS réel** (A01-SMTP-1) avec certificat auto-signé de dev (`rcgen`, régénéré à chaque démarrage) — pas une PKI réelle, un vrai client externe rejetterait ce certificat (vérifié : `openssl s_client` signale `verify error:num=18: self-signed certificate`) ; version/chiffrement négociés enregistrés dans `mail.messages.trust_metadata` (exigence explicite d'A01-SMTP-1) | Pas de certificat signé par une autorité pour `w3.tel` dans cette maquette | Certificat réel (Let's Encrypt ou interne), rotation, chaîne de confiance | A01-SMTP-1 |
| MX entrant (A01) | STARTTLS est proposé et fonctionnel mais PAS imposé : un client peut encore envoyer en clair, la session continue quand même | A01-SMTP-1 exige d'OFFRIR STARTTLS, pas de le rendre obligatoire — comportement conforme, mais une politique tenant "TLS requis" n'existe pas | Politique par domaine/tenant pouvant exiger TLS avant d'accepter DATA | A01-SMTP-1 |
| MX entrant (A01) | Pas d'authentification SPF/DKIM/DMARC/ARC (A01 §5) : aucune vérification d'origine n'est faite | Périmètre maquette (nécessite des enregistrements DNS réels sur `w3.tel`, gérés par Cédric) | Évaluation SPF/DKIM/DMARC/ARC, verdicts en métadonnées | A01-AUTH |
| MX entrant (A01) | Pas d'antivirus ni de CDR sur les pièces jointes (A01 §6). Depuis le parsing MIME (`diamy-mail-mime`), les pièces jointes sont désormais DÉTECTÉES (`attachments_seen` en métadonnées) mais volontairement PAS conservées séparément — A01-AV-1 exige un scan AV avant toute conservation ; sans AV, les conserver serait moins sûr que ne pas les conserver du tout. Le corps textuel sélectionné (seul) est chiffré ; le contenu des pièces jointes elles-mêmes est perdu (ni chiffré, ni délivré) | Périmètre maquette — choisi comme le côté sûr-par-construction du compromis, pas un oubli | Scan AV + CDR (A01-AV), puis conservation en blob(s) séparé(s) par pièce jointe (A02-CRY step 7) | A01-AV |
| MX entrant (A01) | **Parsing MIME/RFC 5322 implémenté** (A01-PARSE, step 2) : nouvelle crate `diamy-mail-mime`, wrapper autour de `mail-parser` (bibliothèque Rust sans `unsafe`, fuzzée/MIRI, conçue pour du courrier hostile — même logique que réutiliser `idna`/`aes-gcm` plutôt que réécrire un parseur MIME à la main). Sélectionne UN corps : texte brut authentique en priorité (A08-TXT-1 n'a plus à deviner où finissent les en-têtes), sinon source HTML authentique préservée TELLE QUELLE (non convertie — A08 reste strictement client-side, A01 §1.1), sinon repli sur les octets bruts d'origine (rien n'est jamais perdu silencieusement). Décodage RFC 2047/2231 assuré par `mail-parser` en interne mais non ressorti (aucun en-tête, ex. Subject, n'est exposé en clair côté serveur — le sujet EST du contenu). Bornes : taille totale déjà bornée en amont (`max_data_bytes`), récursion message-dans-message bornée par `mail-parser` (3 niveaux), + borne défensive sur le nombre de parties (`MAX_PARTS=10000`) ; jamais de panique sur entrée adversariale (testé). Vérifié en conditions réelles (SMTP → Postgres → sync → déchiffrement) sur 3 cas : multipart/alternative (texte+HTML) → seul le texte brut est retenu, `trust_metadata.mime_body_source="PlainText"` ; HTML seul → markup brut préservé, `mime_body_source="HtmlSource"` ; le `size_bytes` stocké reflète bien le corps sélectionné (63 octets), pas le message multipart brut (~350 octets) | — | Étendre au-delà du corps unique le jour où l'AV (A01-AV) et le pipeline HTML client (A08) existent tous les deux — voir la ligne AV ci-dessus | A01-PARSE |
| **Correction appliquée (ex-ESCALADE A01/A21, tranchée par Cédric le 2026-07-15)** | La divergence A01/A21 sur la file de hold (le corps ENTIER scellé sous `k_hold` + reconstruit à la release, faute de colonne reliant un hold à un message catalogué — exactement l'erreur nommée A01 §13 err.#8) est **résolue** : Cédric BORNECQUE (référent du projet) a **explicitement validé l'option (a)** — **on amende A21**, pas A01 — suite à l'escalade documentée dans ce fichier. **Distinction auteur / arbitre :** la modification de schéma et sa migration ont été **écrites par une session Claude Code** ; la **décision a été arbitrée par Cédric**, dont la confirmation a été donnée directement à Hugo (communication hors dépôt) — le changelog A21 et cette ligne consignent cette décision, ils n'en sont pas la preuve à eux seuls. **A21 est passé en v1.5** (§2.6 réécrit + changelog daté 2026-07-15, avec cette même distinction auteur/arbitre) pour porter le design **clé seule** d'A01-HOLD-1/5. Implémenté : (1) migration `0004_hold_queue_key_only.sql` (0003 laissé INTACT, déjà appliqué — discipline A21-X-4) : `hold_queue` gagne `message_id` (FK `mail.messages`, `ON DELETE CASCADE`), `ciphertext`→`wrapped_kmsg`, `hold_nonce`→`wrap_nonce`. **Note : la migration 0004 purge (`DELETE`) les lignes `hold_queue` existantes au format 0003 (corps entier sous `k_hold`, sans `message_id`) — structurellement irréconciliables avec le nouveau schéma clé-seule, et purge de toute façon nécessaire pour poser `message_id NOT NULL` ; cohérent avec la discipline re-provision des données `dev-crypto`, mais implique que toute donnée de hold en cours sur un environnement partagé au moment de la migration est perdue** ; (2) à la réception, un destinataire sans appareil actif fait cataloguer le message NORMALEMENT (ligne `mail.messages` + blob de corps sous `k_msg`, `store_held_message`) mais SANS enveloppe d'appareil, et `hold_queue` ne porte plus que `k_msg` emballé sous `k_hold` (`crypto::wrap_message_key_under_hold`) ; (3) `release_held_messages_for_principal` réécrite : elle ne désemballe que `k_msg` (`crypto::unwrap_message_key_from_hold`, JAMAIS `open_with_key`/`open_message`) et produit des enveloppes normales (A02-CRY-4) pour le `message_id` pré-existant — le corps chiffré dans `mail.blobs` n'est ni lu, ni réécrit, ni re-scellé (A01-HOLD-5). Prouvé par trois tests : `diamy-mail-storage::release_never_decrypts_the_body` (analyse statique du call-graph : la fonction ne contient ni `open_with_key` ni `open_message`, et contient bien `unwrap_message_key_from_hold`) ; round-trip réel dans `diamy-mxd::held_message_is_released_on_first_device_enrollment` qui vérifie EN PLUS que le chiffré du corps au repos est **bit-à-bit identique avant/après release** ; et la préservation de `sender_canonical` de bout en bout (voir la ligne dédiée ci-dessous). `diamy-mail-crypto::hold_wraps_only_the_message_key_body_untouched` couvre l'unité crypto. Réutilise le pattern `alg_version` (INV-7) : `unwrap_message_key_from_hold` est le **4e site de déchiffrement** à dispatch `match` sans catch-all (les trois autres : `open_message`/`open_with_key`/`unwrap_key` ; + le site d'écriture `AlgVersion::as_i32`, soit 5 sites au total). Audit (INV-20) : entrée `hold.release` distincte ajoutée sur la libération (IDs + compte d'enveloppes, jamais de clé/contenu — INV-21), à côté de `publish_device_bundle`/`purge_expired_holds`. **Artefact historique** : le commentaire « Flag pour Cédric : A01 §7 et A21 §2 se contredisent … à réconcilier » qui subsiste dans `0003_hold_queue.sql` (lignes 9-10) est **résolu** depuis cette migration 0004 / A21 v1.5 ; il est laissé INTACT dans le fichier `.sql` par discipline de non-modification des migrations appliquées (A21-X-4 : le contenu d'une migration déjà appliquée entre dans son checksum `sqlx` et ne se modifie jamais, même pour un commentaire — le modifier casserait la validation de migration sur toute base l'ayant déjà appliquée). La résolution réelle est consignée dans `0004_hold_queue_key_only.sql` et dans ce fichier, PAS dans le commentaire de 0003 lui-même | — | — | A01-HOLD/A21 §2.6 |
| **Correction appliquée** | `sender_canonical` d'origine était PERDU à la release (l'ancien design sans `message_id` remplaçait l'expéditeur par le placeholder explicite `"expediteur-perdu-lors-du-hold@invalide"`). Réparé au passage du retouchage de schéma (préoccupation distincte du clair) : le message étant désormais catalogué DÈS la réception (design clé seule ci-dessus), `mail.messages.sender_canonical` porte le VRAI expéditeur et il est préservé à travers hold + release — plus aucun placeholder. Testé de bout en bout : le test de release assert `sender_canonical == "expediteur.test@example.fr"` (et `!=` le placeholder) après release | — | — | A21 §2.2/A01-HOLD-1 |
| MX entrant (A01) | File de hold (A01-HOLD, ferme A17-DIR-5) : borne de taille par principal (`MAX_HELD_PER_PRINCIPAL=100`, constante, pas configurable par tenant contrairement à A01-HOLD-3) ; expiration par défaut +30 jours (calculée en SQL) ; purge périodique MAIS sans DSN réel à l'expéditeur (aucun envoi sortant n'existe dans cette maquette) — la purge a bien lieu, seule la notification manque. Release déclenchée par un **balayage périodique** (`diamy-mxd`, toutes les 5s, `HOLD_RELEASE_SWEEP_INTERVAL`) plutôt qu'un vrai système événementiel — parce qu'`enroll_test_device` simule un CLIENT (voir sa doc) et ne peut PAS déclencher la release lui-même : la release manipule `k_hold`, un secret SERVEUR, jamais connu du client (violation de zone sinon). Un vrai système réagirait à la publication réelle du paquet d'appareil (A17-DIR-3, endpoint absent ici). Design **clé seule** (A21 §2.6 v1.5, tranché par Cédric — voir la ligne "ex-ESCALADE" plus haut) : le message est catalogué sous `k_msg` dès la réception, `hold_queue` ne porte que `k_msg` emballé sous `k_hold`, la release ne re-wrappe que `k_msg` (le corps n'est jamais re-manipulé). Vérifié en conditions réelles : un message à `aubin@w3.tel` (zéro appareil) → SMTP `250` (pas `451`) → `hold_queue` +1 ; enrôlement du premier appareil → balayage suivant (~5s) → `hold_queue` -1, message lisible/déchiffrable via `read_test_mail`, `trust_metadata.released_from_hold=true`, `sender_canonical` = le VRAI expéditeur (plus de placeholder) | Périmètre maquette | `k_hold` dérivé par un vrai `diamy-secretd` (Level A, A17-ENC-1) au lieu d'un secret d'env ; DSN réel à expiration ; borne configurable par tenant ; release événementielle depuis le vrai endpoint A17-DIR-3 | A01-HOLD |
| IAM (dev) | `DevIamClient::seeded()` dérive l'`id` de chaque principal par UUIDv5 déterministe (hash de l'adresse), pas par `Uuid::now_v7()` | Un vrai IAM assigne un id stable une fois pour toutes ; cette doublure de dev doit imiter cette stabilité pour que plusieurs process (`diamy-mxd`, l'exemple d'enrôlement) résolvent le même principal — bug trouvé et corrigé en le testant | Id attribué par le vrai IAM à la création du compte, immuable | A17 |
| IAM (dev) | L'`id` de principal dérivé en UUIDv5 n'est pas un UUIDv7 (CDM-ID-1 : identifiants UUIDv7 partout) | Nécessaire pour la stabilité déterministe du stub ; aucun code ne dépend de l'ordonnancement temporel de cet id (traité comme opaque, externe à IAM) | Id UUIDv7 assigné par le vrai IAM | A17/CDM-ID-1 |
| MX entrant (A01) | Pas de groupes de diffusion (A01 §4bis), pas de DSN/bounce par destinataire, codes SMTP simplifiés (551 générique plutôt que la distinction fine du §10) | Périmètre maquette | Expansion de groupe, DSN réels, codes fins par cas d'échec | A01-GRP / A01 §10 |
| Rendu (A08) | Nouvelle crate `diamy-mail-render` : seul le chemin **text/plain** d'A08 est implémenté (A08-TXT-1 — découpage en paragraphes sur lignes vides, retour à la ligne simple = `hardBreak`, auto-détection de lien restreinte à un préfixe de schéma whitelisté A08-URL-1 `https/http/mailto/tel`, `schema_version` A08-SCH-2, NFC A08-STAB-3, conversion pure/déterministe A08-PIPE-2). Câblé dans la démo `read_test_mail` : le clair vérifié n'est plus jamais affiché brut, seul le document Tiptap + sa projection texte le sont (INV-17). Vérifié de bout en bout avec un vrai message envoyé par SMTP à `diamy-mxd` | Le parsing MIME (`diamy-mail-mime`) sait désormais détecter un corps HTML et le préserve (non converti), mais AUCUN pipeline HTML→Tiptap client n'existe encore (A08 §3-§9 : pré-nettoyage, hidden-content, tables, CID, images distantes) — un mail HTML-only traverse maintenant toute la chaîne, mais s'affiche comme du markup brut inerte dans la démo (honnête, pas caché) | Pipeline HTML complet d'A08 (pré-nettoyage, DOM borné §10, élagage contenu caché §5, classification/aplatissement de tables §7, résolution CID §8, passerelle image distante) ; sandbox "view original" A09 (dépend du HTML) ; auto-détection de lien par vraie grammaire URI (RFC 3986) au lieu d'un préfixe de schéma sur jeton délimité par espaces | A08/A09 |
| MX entrant (A01) | La structure intermédiaire `mail_parser::Message` (en-têtes décodés, arbre de parties, texte/HTML décodé) n'est PAS explicitement zeroïsée avant désallocation — seuls `plaintext` (octets bruts reçus) et `parsed.body` (corps sélectionné) le sont explicitement (A01-DESTROY-1 les couvre, pas les structures internes de la bibliothèque de parsing tierce) | `mail-parser` est une bibliothèque générique sans notion de zeroization ; l'envelopper pour zeroïser chaque `Cow<str>`/`Vec<u8>` interne serait significatif pour un gain de défense-en-profondeur (fenêtre mémoire transitoire, pas une fuite structurelle : rien n'est jamais persisté) | Wrapper zeroïsant autour de `mail_parser::Message`, ou allocateur zeroïsant global en prod | A01-DESTROY-1 |
| Observabilité (A22, guide §7 étape 0) | `diamy-mxd` expose désormais `/metrics` (`:9102`, absent avant — seul `diamy-maild` en avait un) avec des compteurs/jauges RÉELS branchés sur le pipeline : `message_received`/`message_delivered`/`message_held`/`recipient_rejected`/`message_rejected_oversized`/`tempfail_{directory,crypto,storage,hold_full,hold_error}_error`/`hold_released`/`hold_expired_purged` (compteurs), `hold_queue_depth` (jauge, mise à jour à chaque passage du balayage de release). `diamy-obs` gagne une seconde famille de métriques (`diamy_gauges`, `IntGaugeVec`) à côté de l'unique compteur générique déjà existant (`diamy_events_total`, qui ne portait jusqu'ici qu'un événement "startup"). Prometheus scrape maintenant les deux services (labellisés `diamy_service` pour un panneau "cibles up") ; Grafana provisionne AUTOMATIQUEMENT (au démarrage, sans clic dans l'UI) une datasource Prometheus + un dashboard "Diamy Mail" (12 panneaux : débit par résultat, profondeur de la file de hold, relâchements/purges, activité `diamy-maild`, cibles up) — voir `deploy/grafana/`. Vérifié en conditions réelles : traffic SMTP généré (livré/tenu/rejeté), requêtes Prometheus ET via le proxy datasource de Grafana (le chemin réel qu'emprunte le dashboard) confirmant des valeurs exactes | Périmètre maquette : pas de couverture complète du contrat d'observabilité d'A01 §11 (pas de latences/histogrammes `mxd_pipeline_duration`, pas de jauge PAR TENANT pour la profondeur de hold — une jauge globale seulement, pas d'indicateurs de santé distincts des métriques métier au sens strict d'OBS-1) ; `diamy-maild` ne gagne aucun nouveau compteur granulaire (toujours le seul `metrics_scrape`) — resté hors périmètre pour ne pas diluer l'effort | Histogrammes de latence par étape du pipeline (A01 §11), jauge de profondeur de hold PAR TENANT, indicateurs de santé distincts (OBS-1/A22), compteurs de requêtes de l'API de sync (`diamy-maild`) | A22/A01 §11 |
| CI / fuzz (A18) | **Aucune CI ni fuzzing.** Le corpus qualifie ces éléments de *gates de release* : `cargo audit` + `cargo deny` en CI (A18-CI-1), `clippy` en deny-warnings + suites test-vector/fuzz-smoke/negative en gate (A18-CI-3), fuzzing des parseurs SMTP/MIME/HTML via `cargo-fuzz` (A18-TEST-3), et le forbidden pattern #13 (« skipping test-vector / fuzz / negative tests as later — they are release gates »). Rien de tout cela n'existe dans le dépôt : pas de workflow CI, pas de cible `cargo-fuzz`, `clippy -D warnings` non imposé. Gap **release-blocking selon le corpus**, non traité dans le périmètre actuel de la maquette (chantier séparé, plus lourd) — documenté ici honnêtement, pas comblé | Chantier séparé, non couvert par ce périmètre | Pipeline CI (`cargo audit`/`cargo deny`/`clippy -D warnings` + suites test-vector/fuzz-smoke/negative en gate) et corpus `cargo-fuzz` sur les parseurs hostiles (SMTP/MIME/HTML) | A18-CI-1/3 / A18-TEST-3 / forbidden #13 |
| Modèle mort (`diamy-mail-model`) | La crate `diamy-mail-model` (`StoredMessage`/`StoredEnvelope`/`field_class`) n'est **branchée nulle part** : aucun crate (service ou lib) n'en dépend, le stockage réel passe par `diamy-mail-storage` + l'object store. Deux désalignements avec l'implémentation réelle : (1) `StoredMessage` porte `body_ciphertext: Vec<u8>` **inline**, alors que le corps chiffré vit dans l'object store (`mail.blobs.object_key`, A21 §1.1) et que Postgres n'en garde que la référence — le modèle décrit un stockage inline qui n'existe pas ; (2) `field_class` (doc vivante + base d'un futur linter INV-2) n'a **aucun appelant** : c'est une déclaration inerte, elle ne contraint aucun chemin d'exécution. **Dette de modèle, pas un bug de sécurité** (rien de tout ceci ne s'exécute). Non refactoré : le faire serait une redéfinition du type sans appelant contre lequel prouver quoi que ce soit — documenté plutôt que touché. NB : la classification interne de `field_class` a été corrigée pour coller à A02 §4.3 (kem_ct/wrapped_key = PLAINTEXT_METADATA) et verrouillée par un test, indépendamment de son absence de câblage | La tranche verticale utilise `diamy-mail-storage`, jamais ce modèle | Aligner `diamy-mail-model` sur le stockage réel (référence de blob au lieu de `body_ciphertext` inline) et câbler `field_class` comme base du linter INV-2, ou retirer la crate si elle reste sans usage | A21 §1.1 / INV-2 |
| Agenda | Absent | Hors périmètre tranche 1 | diamy-cald | A12-A15 |
| **Bridge (A20) — nouveau composant, tranche démo minimale** | `services/diamy-bridged` : serveur IMAP local (`LOGIN`/`SELECT INBOX`/`LIST`/`FETCH`/`UID FETCH`/`STORE`/`UID STORE`/`EXPUNGE`) pour connecter un client tiers (Thunderbird) à la maquette — **plus purement lecture seule depuis l'implémentation de l'état lu/supprimé réel (A04 §3/§5.3/§6, voir la ligne dédiée plus bas)** : `\Seen`/`\Deleted` sont désormais de VRAIS flags serveur-autoritaires. Simplifications ASSUMÉES par rapport à A20 complet : (1) **pas de TLS** sur loopback (A20-ARCH-4 est un *SHOULD*, dont le modèle d'échec §9 couvre explicitement ce repli : "Loopback TLS unavailable in client → Plaintext-on-loopback with documented exposure") ; (2) **un seul compte préconfiguré PAR INSTANCE** (`DIAMY_BRIDGED_IMAP_USER`/`_PASSWORD`, valeurs fixes) au lieu d'un mot de passe Bridge généré/révocable PAR CLIENT (A20-CRED-1/3) — aucune UI de génération/révocation, aucune énumération de clients connectés. **Plusieurs comptes de démo en parallèle = plusieurs instances**, une par utilisateur, chacune sur ses propres ports (`DIAMY_BRIDGED_IMAP_PORT`/`DIAMY_BRIDGED_SMTP_PORT`, défauts inchangés 1143/1587) — c'est le modèle prévu pour simuler un échange entre deux comptes, pas un contournement (voir `DEMO_GUIDE.md` §6, "Scénario avancé : échange entre deux comptes") ; (3) **une seule boîte** (INBOX), pas de multi-dossier — `STORE`/`UID STORE` (`\Seen`/`\Deleted` uniquement, voir la ligne dédiée plus bas) et `EXPUNGE` (purge dure) SONT désormais réels et persistés côté serveur ; `COPY`/`APPEND` restent non supportés (aucun dossier cible n'existe pour les recevoir dans ce périmètre mono-boîte) ; (4) **pas de SMTP** (A20-SMTP hors périmètre — `diamy-submitd` lui-même n'est pas implémenté) ni **CalDAV** (A20-CALDAV hors périmètre) ; (5) **pas de surfaçage de confiance IMAP** (A20-TRUST-1/2, ex. en-tête `X-Diamy-Trust`) — gap non comblé, signalé ici plutôt qu'inventé ; (6) **UID stables et persistés — CORRIGÉ** (Point 2, décision de Cédric 2026-07-17) : les UID ne sont plus positionnels/de-session mais résolus depuis une table de correspondance `message_id`→UID PERSISTÉE par principal/mailbox (compteur strictement croissant, jamais réutilisé), `UIDVALIDITY` reste stable, un `EXPUNGE` ne fait plus jamais glisser les UID restants — voir la ligne dédiée « Correction appliquée — UID IMAP stables et persistés » plus bas ; (7) ENVELOPE/Date IMAP construits à partir de la chaîne `received_at` telle que renvoyée par l'API de sync (format `Display` de `time::OffsetDateTime`), PAS reformatés au format RFC 5322 strict — lisible mais non strictement conforme ; (8) pas de littéraux IMAP en entrée (`{n}\r\n...`) : `LOGIN` n'accepte que des tokens nus ou entre guillemets ; (9) compteurs `diamy_obs::Obs` incrémentés en mémoire (sessions, opérations IMAP, refus non-loopback) mais **aucun endpoint `/metrics` HTTP** exposé (contrairement à `diamy-mxd`/`diamy-maild`) — visibles seulement via les logs `tracing`, gap vs A20-OBS-1 signalé, pas comblé. **CE QUI EST HONORÉ, pas un raccourci :** le Bridge est son PROPRE appareil IAM enrôlé, avec ses PROPRES clés, distinct de tout autre appareil même co-localisé (A20-CRED-4b — voir `enroll_bridge_device.rs`, fichier de coffre dev SÉPARÉ `*.bridge.devicekey`) ; il porte sa PROPRE AppKey Tier 2 `diamy-mail-bridge`, distincte de celle du client natif de test (A20-CRED-5 — `DIAMY_MAILD_DEV_BRIDGE_APPKEY`, seconde entrée dans `AppKeyStore::seeded_from_env`) ; le loopback-only est câblé en dur, pas configurable (A20-NET-1), vérifié APRÈS bind (A20-NET-2, fail-closed) et par-pair à chaque connexion (A20-NET-3) ; le déchiffrement est LOCAL, avec vérification du tag AVANT tout usage, exactement le chemin de `read_test_mail.rs` (A20-IMAP-1/INV-8) ; le serveur ne reçoit jamais le clair (zéro-accès inchangé, A20-THREAT-3). | Tranche démo volontairement réduite (voir la demande explicite : LOGIN/SELECT/FETCH/LIST seulement, pas d'envoi) | STARTTLS loopback avec certificat local de confiance (A20-ARCH-4), mots de passe Bridge générés/révocables/énumérables par client (A20-CRED-1/3), multi-dossier (`\Seen`/`\Deleted` sont déjà persistants, voir la ligne dédiée), `COPY`/`APPEND`, CalDAV (A20-CALDAV), surfaçage de confiance (A20-TRUST), `/metrics` HTTP (les UID stables à travers un EXPUNGE sont désormais FAITS — voir la correction Point 2) | A20 |
| **Sync (A04 §3/§5.3/§6) — état lu/supprimé, tranche réelle demandée par Cédric (2026-07-17)** | Implémentation RÉELLE (pas un stand-in local au Bridge), tranchée par deux décisions prises directement avec Hugo en session (voir A04 v1.4 changelog — pas des escalades vers Cédric, des gaps de détail d'implémentation dans une direction déjà tranchée, A25 Constitution règle 4) : **CE QUI EST RÉEL** — (1) migration `0005_journal_and_idempotency.sql` : `mail.journal` (A21 §2.5, spécifié depuis A21 v1.0 mais jamais câblé jusqu'ici) copié VERBATIM, + `mail.idempotency_keys` (A21 §2.7, nouvelle table, v1.6) ; (2) `POST /v1/mailbox/:principal_id/state/flags` et `POST /v1/mailbox/:principal_id/state/delete` sur `diamy-maild` (`sync_api.rs`), sous le MÊME middleware d'auth partagé que les endpoints existants (AppKey Tier 2 puis jeton mail-plane, A17-APPKEY-5, réutilisé tel quel, jamais redéfini) ; (3) `deleted` est un champ booléen RÉVERSIBLE de `/state/flags` (A04-EP-4bis, v1.4) — décision prise pour combler un gap réel du texte d'A04 §5.3 (qui ne listait que read/answered/flagged/tags, sans place pour un `\Deleted`-avant-EXPUNGE réversible, ni pour une "annulation" d'`/state/delete`) ; `/state/delete` ne câble que le mode `"hard"` (purge, A02-DEL-1) — `"soft"` est rejeté explicitement en `ERR_VALIDATION` (fail-closed, INV-16), jamais silencieusement mal exécuté ; (4) idempotence RÉELLE (A04-IDEM-1) : `mail.idempotency_keys` stocke la réponse + séquence de la PREMIÈRE exécution, un rejeu sous la MÊME clé ne ré-applique RIEN ; (5) chaque événement d'état (`flags_changed`/`message_deleted`) est journalisé DANS LA MÊME TRANSACTION que la mutation, avec sa séquence monotone (A04-EP-4) ; (6) côté Bridge : `STORE`/`UID STORE` (`\Seen`/`\Deleted` UNIQUEMENT, périmètre explicitement demandé) appellent `/state/flags` avec une clé d'idempotence FRAÎCHE (UUIDv7) À CHAQUE requête — jamais un cache local qui remplacerait l'appel réseau ; `EXPUNGE` appelle `/state/delete` (hard) pour chaque message `\Deleted` de la boîte sélectionnée et émet les `* n EXPUNGE` non tagués avec la décrémentation de séquence correcte (RFC 3501 §7.4.1) ; `SELECT` répond désormais `[READ-WRITE]` avec `PERMANENTFLAGS (\Seen \Deleted)` ; `FETCH FLAGS`/`STATUS UNSEEN` reflètent l'état RÉEL lu en base à chaque interrogation (jamais une valeur figée) ; (7) AUCUN de ces chemins ne touche `summary_ct`/`body_ct`/les enveloppes — uniquement `mail.messages.state_flags` (métadonnée JSONB) et le catalogue lui-même pour la purge (INV-1/2, périmètre non négociable respecté). **CE QUI RESTE SIMPLIFIÉ, honnêtement** : seuls `\Seen`/`\Deleted` sont câblés (pas `\Answered`/`\Flagged`/tags, hors périmètre demandé — A04-EP-5 delta-tags non concerné) ; la résolution de conflit **per-field LWW côté CLIENT** (A03-SYNC-1/2, propriété du VAULT CLIENT A03, qui n'existe PAS dans ce dépôt) n'est PAS construite — seule l'AUTORITÉ SERVEUR dont elle dépendrait est réelle (séquence de journal monotone assignée atomiquement, payload PAR CHAMP jamais par enregistrement entier) ; aucun scénario d'écriture CONCURRENTE opposée sur le même champ n'est donc exercé (le Bridge tape toujours le réseau en direct, il n'y a pas de file d'attente hors-ligne à rejouer) — les 4 preuves demandées (persistance cross-connexion, rejeu d'idempotence, purge persistée, deux "appareils" sur le même principal) portent sur la RÉALITÉ de la synchronisation serveur-autoritaire, pas sur la résolution de conflit elle-même ; `mail.idempotency_keys` n'a AUCUNE vérification que le corps rejoué correspond au corps original (A21-IDEM-2 : même clé → même résultat renvoyé, quel que soit le corps) ni de purge/rétention (A04-IDEM-1 fixe un plancher ≥24h, pas un plafond — les lignes ne sont jamais supprimées ici) ; la paire vérification-puis-enregistrement de la clé d'idempotence n'est PAS atomique entre les deux étapes (fenêtre de course reconnue, acceptable pour un Bridge séquentiel, pas pour une charge concurrente adversariale) ; pas de signature de requête (A04-SIG-1, gap partagé avec le reste de l'API) ; le journal est désormais ÉCRIT mais RIEN ne le CONSOMME via un curseur (pas de `POST /sync/events`, pas de WSS signal-seul, pas de full-resync — A04-SYNC-1/3/4) ; la limitation UID-recalculé-à-chaque-fetch du Bridge (documentée avant la tranche EXPUNGE) est désormais CORRIGÉE — UID stables et persistés, plus aucun glissement après un `EXPUNGE` (voir la correction Point 2 plus bas) ; la suppression du blob en object store à la purge est VÉRIFIÉE (suppression + contrôle d'inexistence, A02-DEL-2) mais best-effort — un échec n'invalide pas la purge catalogue déjà actée (laissé au GC, esprit A02-FAIL-2). **Preuve, pas affirmation** : 5 tests d'intégration réels sur `diamy-maild` (Postgres réel, HTTPS réel, deux `reqwest::Client` INDÉPENDANTS pour prouver la persistance cross-connexion et le partage entre deux "appareils" du même principal, rejeu d'idempotence avec un corps CONTRADICTOIRE pour prouver l'absence d'effet dupliqué, purge suivie d'un nouveau GET, rejet explicite du mode `"soft"`) + 1 test d'intégration bout-en-bout sur `diamy-bridged` (`imap_store_and_expunge_round_trip_against_real_maild`) qui pilote le VRAI protocole texte IMAP contre un VRAI subprocess `diamy-maild` séparé (jamais modifié, jamais d'appel direct aux fonctions internes) : `LOGIN` → `SELECT` (`[READ-WRITE]`, `PERMANENTFLAGS`) → `STORE +FLAGS (\Seen)` (réponse `FETCH` non-SILENT vérifiée) → `FETCH FLAGS` → `STORE +FLAGS (\Deleted)` → `EXPUNGE` (`* n EXPUNGE` vérifié) → nouveau `SELECT` (`EXISTS` décrémenté) → vérification INDÉPENDANTE directement en base (`mail.messages`) que la ligne a disparu. | Décision explicite de Cédric (implémenter l'option 3, fidélité à A04) ; les deux sous-décisions de wire-format/mapping `\Deleted` ont été signalées comme ambiguës puis tranchées directement avec Hugo en session (A04 v1.4 changelog) plutôt qu'inventées silencieusement | Résolution LWW client réelle une fois un vrai vault client A03 construit ; curseur de sync + WSS signal-seul consommant le journal déjà écrit ; signature de requête (A04-SIG-1) ; vérification du corps rejoué en idempotence ; mode `"soft"` (déplacement vers Corbeille) si un jour le Bridge devient multi-dossier | A04 §3/§5.3/§6, A03-SYNC-1/2, A21 §2.5/§2.7 |
| **Sortant (A10/A20-SMTP-1) — nouveau composant, tranche démo minimale** | `services/diamy-submitd` (auparavant un squelette qui affichait juste "à implémenter") expose désormais `POST /submit` (HTTPS, authentifié AppKey+jeton mail-plane) et `services/diamy-bridged` gagne un second listener **SMTP** (`127.0.0.1` uniquement, A20-ARCH-2/NET-1/2/3 — MÊME discipline loopback que l'IMAP), pour qu'un client tiers (Thunderbird) puisse ENVOYER via le Bridge. **Décision d'architecture lue dans le corpus, pas devinée** : A20-SMTP-1 dit explicitement que le Bridge "runs [le message] through the native outbound path (A04 `/submit` → A10 emission) ... emitting via `diamy-submitd`" — donc le Bridge ne relaie JAMAIS lui-même vers Internet, il délègue systématiquement à `diamy-submitd` via HTTP (voir la doc de module de `diamy_submitd::lib` pour la citation complète et le raisonnement). Simplifications ASSUMÉES par rapport à un vrai A10/A23 (demandées explicitement, périmètre volontairement réduit) : (1) **pas de DKIM** (A10-AUTH-1) — aucune signature n'est apposée ; (2) **pas de vérification SPF/DKIM/DMARC** avant émission (SEC-OUT-2 non enforce, fail-OUVERT plutôt que fail-closed — écart normatif assumé et documenté, pas cavalier) ; (3) **pas de rate limiting/circuit breaker** (A10-RL, SEC-OUT-1) ; (4) **pas d'allocation de pool d'envoi** (A23) — un seul chemin de sortie fixe, configuré par variables d'env ; (5) **pas de copie « Envoyés » chiffrée côté client** (A02 §5.2, A10-PIPE-1) — le message n'est PAS dupliqué dans le dossier Envoyés du destinataire, seule l'émission a lieu ; (6) **pas de retry/backoff ni de DSN** (A10-RETRY) — un échec transitoire ou permanent est simplement renvoyé au client SMTP, pas de file de réessai ; (7) **relais externe DÉSACTIVÉ (fail-closed, décision de Cédric)** — plus aucun envoi vers un domaine hors des domaines locaux : un tel destinataire est REJETÉ proprement, aucune connexion SMTP sortante externe n'est même tentée (donc la question de la résolution MX ne se pose plus) ; voir la ligne dédiée « Correction appliquée — relais externe désactivé » plus bas ; (8) **A10-AUTH-4 non vérifié** — l'authentification (AppKey+jeton) prouve "un appareil Bridge enrôlé avec une session valide", PAS que ce principal possède exactement l'adresse `mail_from` envoyée (aucune vérification de propriété du `From`) ; (9) **zéroisation partielle du clair d'émission** — le buffer `Vec<u8>` brut est zeroïsé après usage côté Bridge ET côté `diamy-submitd` (A10-EMIT-1 esprit), mais la représentation base64/JSON intermédiaire (transport HTTP) n'est PAS elle-même zeroïsée à chaque copie interne (`reqwest`/`serde_json` gèrent leurs propres buffers) — une vraie implémentation utiliserait un transport binaire à faible overhead avec zéroisation de bout en bout. **CE QUI EST honoré, pas un raccourci** : authentification à deux facteurs réelle (AppKey Tier 2 puis jeton mail-plane, même ordre A17-APPKEY-5 que `diamy-maild`, `services/diamy-submitd/src/auth.rs`) ; un VRAI dialogue SMTP sortant par destinataire (EHLO/MAIL FROM/RCPT TO/DATA/QUIT, dot-stuffing correct, `services/diamy-submitd/src/relay.rs`) ; l'isolement des échecs par destinataire (esprit A10-RETRY-3 : l'échec d'UN destinataire n'invalide pas les autres) ; zéro contenu de message dans les logs (INV-21, vérifié : seuls compte de destinataires/taille/statut sont loggués) ; boucle fermée de démo honnête (un destinataire dont le domaine est dans `DIAMY_SUBMITD_LOCAL_DOMAINS`, par défaut `w3.tel`, est réinjecté dans `diamy-mxd` comme un message entrant ordinaire — PAS un contournement caché, c'est le mécanisme documenté qui permet "Thunderbird envoie → Thunderbird reçoit" sans vrai serveur MX externe). **Vérifié en conditions réelles** (pas seulement des tests unitaires) : un test d'intégration (`services/diamy-bridged/src/main.rs::smtp_roundtrip_tests`) drive un VRAI client SMTP avec AUTH LOGIN contre le VRAI serveur SMTP du Bridge, qui relaie via le VRAI routeur HTTPS de `diamy-submitd` (en-process), qui relaie via un VRAI dialogue SMTP vers un VRAI processus `diamy-mxd` séparé (subprocess du binaire déjà compilé, **code de `diamy-mxd` non modifié**), et vérifie que le message est bien arrivé et déchiffrable côté réception (même chemin AAD/`unwrap_key`/`open_message` que le reste du projet) — round-trip complet "Thunderbird envoie → diamy-mxd reçoit", pas une affirmation. | A10/A20-SMTP |
| **Correction appliquée — relais externe désactivé (décision de Cédric, 2026-07-17)** | **Constat de l'audit** : `diamy-submitd`, s'il recevait un mail pour une adresse hors des domaines locaux (`DIAMY_SUBMITD_LOCAL_DOMAINS`, défaut `w3.tel`), déclenchait un VRAI relais SMTP sortant vers l'extérieur (connexion directe `<domaine>:25`), sans DKIM/SPF/DMARC. En boucle fermée de démo ce chemin ne se déclenchait jamais, mais il EXISTAIT dans le code et pouvait s'activer par erreur (faute de frappe dans une adresse, mauvaise config). **Décision de Cédric** : pour la maquette, TOUT envoi doit rester strictement confiné aux domaines locaux (`w3.tel`) — aucun relais vers l'extérieur ne doit être possible, même par erreur. **Corrigé (fail-closed)** : `submit_api.rs` décide le routage par une fonction PURE `SubmitdConfig::route_for` — domaine local → réinjection dans `diamy-mxd` (boucle fermée, inchangée) ; domaine externe → `RelayRoute::RejectedExternalDisabled` → le destinataire est REJETÉ proprement (`status: "rejected_external_relay_disabled"`, message d'erreur explicite mentionnant que le relais externe est désactivé), AVANT toute connexion réseau (aucun dialogue SMTP sortant externe n'est même ouvert). Au niveau de l'API `/submit`, un envoi purement externe donne `accepted=false` → `502 Bad Gateway`. **Côté client SMTP du Bridge, le rejet est prononcé DÈS le `RCPT TO`** (RFC 5321 §3.6.1) avec un `550 5.7.1` explicite (« relais externe désactivé en maquette »), AVANT tout `DATA` : le Bridge connaît ses propres domaines locaux (`DIAMY_BRIDGED_LOCAL_DOMAINS`, défaut `w3.tel`, miroir de `diamy-submitd`) et refuse un destinataire externe au plus tôt — un client comme Thunderbird affiche alors une erreur claire immédiate au lieu de rester bloqué sur « Envoi… » puis d'échouer seulement après le téléversement du corps. La réponse `550` post-`DATA` (`submit_via_diamy_submitd` renvoyant `accepted=false`) reste en défense-en-profondeur, jamais un silence. Le moteur de dialogue SMTP de `relay.rs` reste utilisé mais UNIQUEMENT pour la réinjection locale ; le chemin externe historique n'est réactivable que par `DIAMY_SUBMITD_ALLOW_EXTERNAL_RELAY=1` (fail-closed : absence de variable = pas de relais externe, jamais l'inverse ; jamais positionnée en maquette). **Preuve, pas affirmation** : `submit_api.rs::tests` — `external_recipient_is_rejected_not_relayed` (une soumission vers `test@gmail.com` est REJETÉE, pas relayée ni ignorée ; le rejet est prononcé sans ouvrir de connexion, ce qui rend le test hermétique — aucun accès réseau vers gmail), `route_for_rejects_external_by_default` et `route_for_allows_external_only_when_explicitly_enabled` (la garde fonctionne dans les deux sens). Les tests de `relay.rs` (dialogue SMTP contre un faux MTA loopback) restent valides : ils exercent le moteur générique, toujours utilisé pour le relais LOCAL, jamais un relais externe réel. | — | Un vrai `diamy-submitd` de prod ferait la résolution MX + DKIM/SPF/DMARC + pool d'envoi (A10/A23) — hors maquette | A10/A20-SMTP |
| **Correction appliquée — UID IMAP stables et persistés (décision de Cédric, 2026-07-17)** | **Constat de l'audit** : les UID IMAP du Bridge étaient POSITIONNELS (recalculés à chaque `fetch_mailbox_catalog` en énumérant le catalogue trié, position 1..N), alors qu'`UIDVALIDITY` restait constante = 1. Contradiction avec la RFC 3501 §2.3.1.1 : une `UIDVALIDITY` constante PROMET des UID stables qu'un vrai client peut mettre en cache entre sessions. Bug concret : après un `EXPUNGE` retirant un message du milieu, les suivants étaient renumérotés, et un nouveau message pouvait hériter de l'UID qu'un client avait mis en cache pour un AUTRE message désormais supprimé — une action ultérieure sur cet UID toucherait alors le mauvais message. **Corrigé — option (a) de l'audit (UID stables persistés, PAS un simple bump d'UIDVALIDITY)** : un `UidRegistry` (côté Bridge, fichiers `bridge_state/<principal>.INBOX.uidmap.json`) tient une table de correspondance PERSISTÉE `message_id` (identité stable, déjà utilisée partout) → UID IMAP stable, PAR principal/mailbox, avec un compteur `uid_next` **strictement croissant** (un UID libéré par un EXPUNGE n'est JAMAIS réattribué). **Choix d'architecture (justifié, pas deviné)** : les UID IMAP sont une notion PROPRE au protocole IMAP (RFC 3501), pas une donnée métier zéro-accès du serveur mail — A20-IMAP-1 fait du Bridge la couche qui PRÉSENTE les messages en IMAP standard, et A04 identifie les messages par `message_id`, jamais par un UID IMAP ; stocker la table dans `diamy-maild` polluerait le catalogue métier d'un artefact protocolaire — donc elle vit côté Bridge (état local). Un message garde EXACTEMENT son UID de son apparition jusqu'à sa purge (jamais recalculé par position au `SELECT`/`STATUS`/`NOOP`) ; `UIDNEXT` = `uid_next` (jamais `count+1`, qui ignorerait les trous laissés par un EXPUNGE) ; les bornes `UID FETCH`/`UID STORE` sont passées à l'UID MAXIMAL présent, plus au nombre de messages. **`UIDVALIDITY` reste stable** tant que la table est saine ; **logique de secours** : si l'état persisté est corrompu/absent/incohérent au chargement (`load_or_fresh` + `is_coherent`), un état frais est créé avec une `UIDVALIDITY` bumpée (timestamp Unix, monotone entre démarrages → strictement supérieure à tout état antérieur), ce qui force un client à jeter son cache plutôt qu'à risquer une mauvaise correspondance. `EXPUNGE` purge toujours par `message_id` (jamais par position — inchangé, vérifié encore vrai avec le nouveau mécanisme). **Preuve, pas affirmation** : `uid_of_expunged_message_is_never_reassigned_to_a_new_message` reproduit EXACTEMENT le scénario de l'audit contre un VRAI subprocess `diamy-maild` + Postgres et un registre isolé (boîte A/B/C → `UID STORE \Deleted` + `EXPUNGE` de B → arrivée de D → l'UID de B n'est JAMAIS réattribué à D, A et C gardent leurs UID, `UIDVALIDITY` inchangée) ; + tests unitaires `uids_are_stable_and_counter_is_monotonic`, `uid_state_persists_across_registry_instances_same_dir` (survie au redémarrage), `corrupt_or_incoherent_state_triggers_a_fresh_bump`. | — | Multi-dossier (une table par mailbox réelle), rétention/GC des tombstones du registre, éventuel partage de l'état d'UID entre instances du même compte | A20-IMAP-1/RFC 3501 §2.3.1.1 |

## Tests automatisés (`cargo test --workspace`)

Nécessitent un Postgres de dev actif (`docker compose up`) : `diamy-mxd` (6 tests, dialogue
SMTP réel contre un vrai serveur en écoute sur un port éphémère, dont un test STARTTLS
complet : négociation TLS réelle, session chiffrée, métadonnées TLS vérifiées),
`diamy-maild` (3 tests, API de sync réelle par HTTPS), et `diamy-bridged` (round-trip complet
du chemin sortant, `smtp_roundtrip_tests::full_round_trip_...` — nécessite EN PLUS que
`diamy-mxd` soit déjà compilé, `cargo build --workspace`/`-p diamy-mxd` : ce test lance un vrai
subprocess du binaire, jamais son code source directement, et le termine en fin de test).
Discipline d'isolation contre la base
partagée (potentiellement concurrente entre binaires de test) : **jamais de `TRUNCATE`**
global ; chaque test génère son propre `device_id`/`message_id` frais et retrouve SON
message par un marqueur unique dans le contenu déchiffré plutôt que par comptage ou "le plus
récent" ; `aubin@w3.tel` est réservé comme principal "jamais enrôlé" dans toute la suite.
Vérifié stable sur plusieurs exécutions complètes consécutives de `cargo test --workspace`.

Le vérificateur de certificat "accepte tout" (`AcceptAnyCert`/`danger_accept_invalid_certs`)
n'est utilisé QUE côté client de test/exemple, jamais côté serveur — un client de test se
connecte à son propre serveur éphémère avec un certificat auto-signé qu'il ne peut pas
vérifier via une vraie chaîne de confiance ; ce n'est pas un contournement de sécurité côté
serveur (le serveur, lui, présente toujours un certificat et négocie toujours un vrai TLS).

## Garde-fous en place
- `dev-crypto` **refuse de démarrer hors `DIAMY_ENV=dev`** (`assert_backend_allowed_for_env`, A18 SEC-FC-1).
- Les données `dev-crypto` **ne sont pas interopérables** avec `messaging-crypto` → re-provision à la bascule, pas de migration.
- La démo `diamy-maild` **vérifie** (contre le vrai Postgres + l'object store local) qu'aucune ligne `mail.messages`/`mail.blobs` ne contient le clair d'origine (INV-1) ; le digest `mail.blobs.sha512_ct` est calculé sur le CHIFFRÉ, jamais le clair (A21-BLOB-1, A21-X-3).
- Le DDL de `crates/diamy-mail-storage/migrations/0001_mail_core.sql` est copié **verbatim** depuis A21 §2.1-2.4 (la DDL prévaut, A21 §1) ; `diamy-mxd` ET `diamy-maild` partagent la même crate `diamy-mail-storage`, donc le même catalogue Postgres — pas deux implémentations qui pourraient diverger (esprit de l'erreur IA n°1 d'A24 appliqué au stockage).
- `diamy-mxd` borne réellement la mémoire par ligne ET par message (A01-STAB-1) : une ligne SMTP sans retour à la ligne au-delà de 8 Ko est drainée sans être stockée, un corps DATA au-delà de 10 Mo est rejeté (`552`) — testé avec un message de 11 Mo, qui est bien rejeté et ne laisse aucune trace en base.
- Isolation des échecs par destinataire (A01-PIPE-3) : un destinataire inconnu est rejeté (`550`) sans bloquer les autres ; le SMTP `250` n'est renvoyé qu'après que la persistance Postgres a committé (A01-PIPE-1).
- Chemin vertical vérifié entre VRAIS processus séparés (pas une démo qui joue les deux rôles) : un mail envoyé par SMTP à `diamy-mxd` est lu et déchiffré par `read_test_mail`, un second processus qui ne parle qu'au réseau à `diamy-maild` et ne connaît que sa propre clé privée locale.
- TLS vérifié avec des clients INDÉPENDANTS de notre propre code de test : `openssl s_client -starttls smtp` négocie bien STARTTLS contre `diamy-mxd` (TLS 1.3, `TLS_AES_256_GCM_SHA384`, certificat signalé self-signed comme attendu) ; un client HTTPS Python indépendant négocie bien contre l'API de sync de `diamy-maild` (même suite). Les deux confirment le protocole réel, pas seulement notre propre harnais.
- L'API de sync rejette toute requête sans AppKey Tier 2 valide **avant** d'examiner le jeton mail-plane (A17-APPKEY-5) : testé avec une AppKey invalide + un jeton lui-même invalide → l'erreur observée est bien `ERR_APPKEY_INVALID`, jamais `ERR_TOKEN_INVALID` (si le jeton avait été vérifié en premier, l'erreur observée aurait été différente — c'est la preuve de l'ordre, pas une affirmation).
- `body_ct`/`summary_ct` sous `k_msg` portent désormais une AAD liée à leur `message_id`/`blob_id` exact (A02-CRY-2/3) : testé qu'un chiffré scellé sous l'AAD d'UN message échoue à s'ouvrir sous l'AAD d'un AUTRE (tag GCM rejeté, fail-closed).
- `alg_version`/`blob_alg_version` (A02-CRY-7) sont écrits au scellement ET **contrôlés au
  déchiffrement** (INV-7) : `AlgVersion::from_i32` rejette *fail-closed* toute version inconnue
  (côté serveur au fetch, côté client sur le fil) ; le dispatch de déchiffrement est un `match`
  sans catch-all — l'ajout d'une variante `V2` casse la compilation aux **4 sites de
  déchiffrement** + le site d'écriture `AlgVersion::as_i32` (**5 au total**) tant qu'elle
  n'est pas traitée explicitement (vérifié : `error[E0004] non-exhaustive patterns`).
- Sink d'audit distinct (INV-20) : `diamy_obs::audit` écrit `actor`/`before`/`after`/timestamp
  dans un fichier JSON-lines dédié (jamais le log applicatif), pour `publish_device_bundle` et
  `purge_expired_holds` ; before/after ne portent qu'une EMPREINTE de clé publique, jamais la
  clé brute ni de contenu (INV-21). Entrées réelles observées pendant `cargo test --workspace`.
- Deux commentaires de code périmés corrigés (état réel, pas revendication) : `active_device_keys`
  (la file de hold EST implémentée) et l'en-tête de `diamy-mail-render` (le parsing MIME existe ;
  le contenu DATA n'est plus un « bloc opaque »).
- Le test anti-régression `no_keygen_in_binary.rs` (INV-4/INV-9/A17-KEY-2) couvre
  `services/diamy-maild/src/main.rs` **uniquement** — **pas** `services/diamy-mxd/src/main.rs`.
  `diamy-mxd` ne génère AUCUNE clé sur son chemin de production (vérifié : ses seuls appels
  `generate_device_keypair`/`generate_identity_keypair` vivent dans `#[cfg(test)]` ; le chemin
  de prod se contente de LIRE l'annuaire via `active_device_keys`, il ne génère jamais). Mais
  aucun garde-fou automatique n'empêcherait un futur changement d'en introduire un dans le
  `main.rs` de `mxd` — asymétrie assumée, à combler en étendant ce test à `diamy-mxd` (voire
  aux trois services) le jour utile.
- `RLIMIT_CORE` est réellement mis à 0 en prod (vérifié par `getrlimit` après appel, pas seulement "pas d'erreur retournée") et laissé INTACT en dev ; le démarrage échoue si la désactivation échoue en prod (fail-closed, A18-ZERO-4).
- Rendu Tiptap (A08, chemin text/plain) vérifié de bout en bout avec un VRAI message : envoyé par SMTP (Python `smtplib`, un processus tiers) à `diamy-mxd`, chiffré, stocké, tiré et déchiffré par `read_test_mail`, qui produit désormais un document Tiptap JSON — un paragraphe multi-lignes reste un seul nœud `paragraph` avec des `hardBreak` (pas fusionné, pas éclaté en paragraphes), et un lien `https://w3.tel/demo.` suivi d'un point voit le `.` correctement exclu du `href` et réémis comme texte séparé.
- Parsing MIME (A01-PARSE, `diamy-mail-mime`) vérifié de bout en bout avec de VRAIS messages envoyés par SMTP à `diamy-mxd` : un `multipart/alternative` (texte + HTML) est stocké avec SEULEMENT la partie `text/plain` comme corps chiffré (`trust_metadata.mime_body_source="PlainText"`, `size_bytes=63`, alors que le message multipart brut fait ~350 octets — les en-têtes/HTML/frontières MIME ne sont plus jamais dans le clair chiffré) ; un message HTML-only est stocké avec `mime_body_source="HtmlSource"` et le markup brut préservé (vérifié via `read_test_mail`, qui l'affiche non converti, cohérent avec l'absence du pipeline A08 HTML) ; vérifié aussi que `trust_metadata` en base ne contient jamais de contenu, seulement les verdicts de parsing (INV-21).
- File de hold (A01-HOLD) vérifiée de bout en bout, en conditions réelles, avec le VRAI cycle complet : un message SMTP à `aubin@w3.tel` (zéro appareil actif, confirmé via `keydir`) reçoit un VRAI `250` (jamais `451`) et `mail.hold_queue` gagne une ligne pour ce principal ; `enroll_test_device -- aubin@w3.tel` publie son premier appareil ; le balayage périodique de `diamy-mxd` (logs observés : "message tenu en attente" puis, ~5s plus tard, "messages relâchés depuis la file de hold released=1") relâche automatiquement le message SANS intervention manuelle ; `mail.hold_queue` retombe à 0 pour ce principal ; `read_test_mail -- aubin@w3.tel` déchiffre et rend en Tiptap le contenu exact envoyé, avec `sender_canonical` = le VRAI expéditeur (design clé seule A21 v1.5 : le message est catalogué dès la réception, l'expéditeur est préservé — plus de placeholder) et `trust_metadata.released_from_hold=true`. Le test d'intégration prouve EN PLUS que le chiffré du corps au repos est bit-à-bit identique avant/après release (le corps n'est jamais re-manipulé, A01-HOLD-5).
- AAD de l'enveloppe (A02-CRY-4) vérifiée en conditions réelles : un message frais envoyé par SMTP à `diamy-mxd` après le câblage de `aad_for_envelope` traverse tout le chemin (chiffrement, enveloppe, stockage, sync, désemballage avec l'AAD reconstruite, ouverture) et se déchiffre correctement — pas seulement le test unitaire de non-régression (`envelope_mismatched_aad_is_rejected`).
- Dashboard Grafana vérifié en conditions réelles, bout en bout (pas juste "le JSON est valide") : `docker compose up` provisionne la datasource (`GET /api/datasources` confirme `Prometheus`/`uid=prometheus`) et le dashboard (`GET /api/dashboards/uid/diamy-mail` confirme les 12 panneaux) sans aucun clic dans l'UI ; `GET /api/v1/targets` de Prometheus confirme `diamy-maild`/`diamy-mxd` tous deux `up` ; du VRAI trafic SMTP (livré/tenu/rejeté) a été généré puis retrouvé avec les valeurs exactes attendues en interrogeant Prometheus ET en repassant par le proxy datasource de Grafana (`/api/datasources/proxy/uid/prometheus/...`, le chemin réel qu'emprunte un panneau, pas un raccourci de vérification).
